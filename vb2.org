#+TITLE: vb2 - vocaBull 2

* Brainstorming with Claude
#+begin_src 
I'd like to start a new project with SQLALchemy+Pydantic+Flask+TS+Vue+Tailwind+DaisyUI.  It's a facelift/remake of my old project vbs which is a flashcard web app with Flask+Jinja2+Bootstrap.  It's hosted on pythonanywhere but it has gotten slow possibly because of throttling for the free tier.  I want to move most flashcard logic to frontend with vb2.

I'd like to auth with gmail.

I'd add tests for vb2.
#+end_src

*** Auth flow
1. Frontend uses Google's Identity Services JS library (the "Sign in with Google" button) to get the user to authenticate directly with Google in the browser.
2. Google hands the frontend a signed ID token (a JWT from Google, containing email, name, etc.) — no redirect back-and-forth through your Flask server needed.
3. Frontend POSTs that ID token to your Flask backend (e.g. /auth/google).
4. Backend verifies the token's signature against Google's public keys (via the google-auth Python library's id_token.verify_oauth2_token), extracts the email, looks up or creates a User row.
5. Backend issues its own JWT (via flask-jwt-extended or manually with pyjwt) for subsequent API calls — this is the token your Vue app stores and sends as Authorization: Bearer ... on every request after that.

*** Skeleton
#+begin_src 
vb2/
├── backend/
│   ├── app/
│   │   ├── models/
│   │   │   ├── user.py
│   │   │   ├── word.py
│   │   │   └── ...
│   │   ├── schemas/
│   │   │   ├── user.py
│   │   │   ├── word.py
│   │   │   └── ...
│   │   ├── routes/
│   │   │   ├── auth.py       # /auth/google
│   │   │   ├── sync.py       # sync endpoint(s)
│   │   │   └── ...
│   │   ├── auth/
│   │   │   ├── google.py     # verify Google ID token
│   │   │   └── jwt.py        # issue/verify your own JWT
│   │   ├── db.py
│   │   ├── config.py         # dev/test/prod configs
│   │   └── __init__.py       # create_app()
│   ├── tests/
│   │   ├── conftest.py       # app + db fixtures
│   │   ├── test_auth.py
│   │   ├── test_sync.py
│   │   └── ...
│   ├── migrations/           # Alembic
│   └── run.py
├── frontend/
│   └── ...
├── Caddyfile
└── docker-compose.yml
#+end_src

*** endpoints
- POST /auth/google — exchange Google ID token for JWT
- GET /books — list user's books
- POST /books — create book
- GET /books/{id} — full load (words + content + practice-state)
- DELETE /books/{id} — delete book
- POST /books/{id}/words — add word
- DELETE /books/{id}/words/{word_id} — remove word
- POST /books/{id}/sync — reconcile full book (content + practice-state, per-word timestamps)

- context (7/19)
#+begin_src 
VB2 PROJECT CONTEXT

Remake of vocaBull (vbs): flashcard web app, Flask+Jinja2+Bootstrap on
PythonAnywhere free tier, currently slow. vb2 moves most flashcard logic
to frontend.

STACK
- Backend: Flask (app factory pattern) + SQLAlchemy + Pydantic + flask-pydantic
- Frontend: Vue 3 + TypeScript + Pinia + DaisyUI
- Auth: Google Sign-In (Identity Services JS) -> ID token -> backend verifies
  via google-auth library -> backend issues own JWT (flask-jwt-extended or
  pyjwt) for API calls. Multi-user.
- DB: SQLite initially, deployed on PythonAnywhere free tier (works fine:
  SQLite is local, Google OAuth verification works since googleapis.com is
  whitelisted, static Vue build served via PythonAnywhere static file mapping).
  Future: may migrate to Postgres, likely self-hosted on homelab (N150
  Ubuntu/Proxmox, used today only as git origin/rsync target) once needed,
  since Postgres wire protocol is blocked on PythonAnywhere free tier.
  Homelab already runs Caddy (reverse proxy/TLS) for existing blog site.
- Tests: pytest, in-memory/throwaway SQLite for test config, mock Google
  token verification in tests.

SKELETON (backend)
backend/app/{models,schemas,routes,auth}/, db.py, config.py, __init__.py
(create_app factory), tests/, migrations/ (Alembic)

DOMAIN CONCEPT (from vbs, being redesigned)
- Multiple books per user (library/borrow-book feature deferred, not in v1)
- Each book: words with content (word/definition/example) + practice-state
  (which "bucket": learning window / waiting window / memorized-book, plus
  SRS-style due counter)
- vb1's Shifting Learning Window (SLW) method being hybridized with SRS:
  frequency-based learning/waiting windows for actively-studied words,
  time-based spaced repetition once "memorized" -- using DUE COUNTERS
  (incremented only on session days) rather than absolute due dates, to
  avoid due-pileup after skipping days/weeks.

ENDPOINTS DECIDED SO FAR
- POST /auth/google              - exchange Google ID token for JWT
- GET  /books                    - list user's books
- POST /books                    - create book
- GET  /books/{id}               - full load (words + content + practice-state)
- DELETE /books/{id}             - delete book
- POST /books/{id}/words         - add word
- DELETE /books/{id}/words/{id}  - remove word
- POST /books/{id}/sync          - reconcile full book (see below)

SYNC DESIGN (decided)
- Client sends/receives ENTIRE book both on load and on sync (not deltas) --
  acceptable since book size is small (tens-hundreds of words) and N150/
  PythonAnywhere can handle full-book JSON easily.
- Reconciliation is per-word, last-write-wins by timestamp, with THREE
  independent timestamp levels (not one per word):
  1. book-level metadata (name, language, etc.) -- own last_updated
  2. word content (word/definition/example) -- own last_updated
  3. word practice-state (bucket, due counter, etc.) -- own last_updated
  This is needed because multi-device use (phone + laptop) means both could
  change different things between syncs; whole-record overwrite would lose
  work. Content edits are rare but still must not be clobbered.

NEXT STEP (in progress when this context was cut)
Designing models.py -- starting with User model fields (beyond Google's
email/name).

WORKING STYLE (important)
Socratic, one problem at a time, concise replies. Kyosuke writes all code
himself -- do not provide code unless explicitly requested. Don't pile on
multiple follow-up questions or extra info beyond what was asked.
#+end_src

*** Scaffolding
#+begin_src 
cd ~/py/vb2
uv venv
uv init
uv add sqlalchemy pydantic
uv add flask flask-sqlalchemy authlib flask-jwt-extended flask-cors
uv add flask-openapi3 flask-openapi3-swagger
uv add python-dotenv
uv add google-auth requests
uv add --dev pytest
uv pip list
#+end_src

*** How to calculate time lapse
- due_dates - the dates for the word to become due.  1 \to 2 \to 4 \to 8 ..
- due_counter - initial value = due_dates.  decrements every day.  0 = due.
- When starting to practice a book, for every word in the book:
  - if it's the next or later day, decrement due_counter by 1
    - Note: it's not >= 24 hours.  I want to know if it's the next day (or later)

- context 7/20
#+begin_src 
VB2 SESSION CONTEXT (continuation)

MODELS.PY - DECIDED (models.py itself already drafted by Kyosuke)
- User: id, sub (Google sub, unique), email (unique), name, created_at (tz-aware)
- Book: id, last_edited (tz-aware, book metadata timestamp), wd_last_practiced,
  dw_last_practiced (date used to trigger due_counter decrement per direction),
  user_id FK
- Word: id, word, definition, sample, last_edited (tz-aware, content-only
  timestamp), book_id FK
- Practice (separate table, NOT columns on Word): id, direction ("wd"/"dw"),
  status (Pracstat: new/learning/waiting/review), due_dates, due_counter,
  last_edited (tz-aware -- updated both by actual practice events AND by
  day-lapse due_counter decrements; serves as sync LWW timestamp), user_id FK,
  word_id FK, UniqueConstraint(direction, user_id, word_id)
  Rationale for separate table: prepares for future library/borrow-book
  feature where multiple users share a book's Word content but need
  independent practice progress. word/definition -> two directions (wd, dw)
  tracked as fully independent progress (separate status/due_dates/
  due_counter/last_edited each), not shared or blended.

DUE-COUNTER / TIME-LAPSE MECHANIC - DECIDED
- due_dates: target interval value (grows 1 -> 2 -> 4 -> 8...) set when word
  status changes/graduates
- due_counter: starts at due_dates, decrements toward 0; 0 = due
- Decrement trigger: compare TODAY's calendar date vs book's
  wd_last_practiced/dw_last_practiced (per direction). If today is later
  (not >=24hrs -- literal calendar-day-later), decrement ALL words'
  due_counter for that direction by 1 (floor 0), in one pass at session
  start (instantaneous, few hundred rows). This is the whole point of using
  a counter instead of absolute due-dates: skipping a week only costs -1,
  not -7, avoiding due-pileup.
- Book-level last_practiced (per direction) drives this, NOT per-word --
  deliberately chosen over per-word so untouched words still decay properly
  even if not touched every session.

SYNC / RECONCILIATION ARCHITECTURE - DECIDED
- Reconciliation happens at TWO points, both real:
  1. CLIENT-SIDE: on every load (GET /books/{id}), client compares its
     localStorage copy against server response per field-group timestamp
     (book metadata / word content / word practice-state), keeps newer,
     produces merged local state.
  2. SERVER-SIDE: on /sync (POST), server does lightweight per-field-group
     LWW check against its own stored timestamps before overwriting (NOT
     full reconciliation of two other copies -- just guards against two
     near-simultaneous device syncs clobbering each other). Server returns
     the reconciled result.
  3. Client then reconciles its local copy again against that /sync
     response (closes the loop, symmetric with GET flow).
- Rationale: client-only reconciliation was insufficient because two
  devices syncing near-simultaneously could have one's reconciled write
  clobber the other's, since by definition each device only reconciles
  against what it personally fetched.

LOCAL STORAGE STRATEGY - DECIDED
- Path 1 chosen explicitly: NOT building offline-mode. Server round-trip
  still happens on every "list books" / "load book" action.
- localStorage holds ONE book at a time (words + practice-state, both
  directions) -- NOT the book list (book list always fetched fresh from
  server, cheap/rarely changes).
- Local (Pinia, backed by localStorage) is authoritative only WITHIN a
  session, between load points -- exists specifically to survive page
  reload without losing in-progress practice progress (Pinia state alone
  is memory-only, wiped on reload).
- Written to localStorage after every card-answer event (cheap/instant,
  full-book JSON, no granular per-word writes -- consistent with
  whole-book-not-deltas design already used for server sync).
- Switching to a different book or direction session: sync current book to
  server FIRST (blocking), only then clear localStorage and load the new
  book. If sync fails, block the switch entirely -- switching requires
  fetching the new book's data anyway, so this isn't an extra guard, it's
  inherent to the flow.

STILL OPEN / NEXT STEPS
- schemas.py: not yet started. Need Pydantic schemas for BookOut (nested
  words + practice-state, both directions) matching the whole-book
  wire format. Was about to start when reconciliation-architecture
  discussion (above) took over -- schemas should reflect the now-settled
  sync design.
- /sync endpoint request/response shape not yet formalized (know it needs
  to accept client's reconciled book and return server's reconciled book,
  per points above).
- Alembic: explicitly deferred until first PythonAnywhere deploy; using
  create_all() + delete/recreate DB during active model iteration.

WORKING STYLE (unchanged)
Socratic, one problem at a time, concise replies. Kyosuke writes all code
himself -- do not provide code unless explicitly requested. Don't pile on
multiple follow-up questions or extra info beyond what was asked.
#+end_src

*** pyright is soooooo annoying.  Which would you choose?
- option 1 - verbose
- option 2 - pyright complains books=books
- option 3 - less, but still verbose.  why should I!?
#+begin_src 
@app.get("/books")
def list_books():
    books = db.session.execute(select(Book).where(Book.user_id == 1)).scalars().all()
    book_schemas = [BookSchema.model_validate(b) for b in books]
    return ListBooksResp(books=book_schemas).model_dump(mode="json")


@app.get("/books2")
def list_books2():
    books = db.session.execute(select(Book).where(Book.user_id == 1)).scalars().all()
    return ListBooksResp(books=books).model_dump(mode="json")


@app.get("/books3")
def list_books3():
    books = db.session.execute(select(Book).where(Book.user_id == 1)).scalars().all()
    return ListBooksResp.model_validate({"books": books}).model_dump(mode="json")
#+end_src

- context 7/21
#+begin_src 
VB2 SESSION CONTEXT (continuation)

STACK: Flask + Flask-SQLAlchemy + SQLite, Pydantic schemas, JWT planned
(not yet built). uv package manager.

MODELS.PY - DECIDED
- Base class: custom Base(MappedAsDataclass, DeclarativeBase), passed to
  SQLAlchemy(model_class=Base) in database.py -- NOT db.Model directly.
  Reason: MappedAsDataclass conflicts with db.Model's DefaultMeta
  (metaclass conflict). This combo avoids it while keeping db.session,
  db.init_app, config-driven engine setup.
- Because of MappedAsDataclass: every Mapped[] field becomes a required
  __init__ kwarg unless explicitly marked init=False (e.g. id primary key,
  relationship fields). Field ordering also matters (non-default before
  default), same as regular Python dataclasses.
- Pracstat and Direction (str, Enum) now live in models.py (moved from
  schemas.py to avoid duplication); schemas.py imports them from models.
- relationship() cascade fix: "all, delete-orphan" (hyphen, not
  underscore) -- typo caused ArgumentError earlier.
- Tables: users, books, words, practices (practices seeded lazily during
  actual learning sessions, NOT in seed.py).

DATABASE.PY - DECIDED
- Minimal: just `db = SQLAlchemy(model_class=Base)`. No app, no
  create_all here -- both moved to api.py.

API.PY - DECIDED
- Owns `app = Flask(__name__)`, config, db.init_app(app), and
  create_all() inside `if __name__ == "__main__":` guard.
- DB path bug (fixed): don't use relative "sqlite:///db.db" -- Flask's
  instance-folder resolution differs based on whether module is run as
  __main__ vs imported (e.g. from seed.py), causing two separate db.db
  files. Fixed via absolute path built from __file__:
    basedir = os.path.abspath(os.path.dirname(__file__))
    app.config["SQLALCHEMY_DATABASE_URI"] = f"sqlite:///{os.path.join(basedir, 'db.db')}"
- Auth: user_id hardcoded to 1 for now (real JWT auth deferred).
- Typing: use plain `from sqlalchemy import select`, not `db.select()`
  (better type inference from basepyright).
- Pydantic/pyright friction pattern -- DECIDED APPROACH: when constructing
  a *Resp schema from ORM objects (e.g. Sequence[Book] -> list[BookSchema]),
  use `ListBooksResp.model_validate({"books": books}).model_dump(mode="json")`
  rather than passing ORM objects directly to the strict __init__ (which
  basepyright flags as type mismatch even though it works at runtime via
  from_attributes=True). model_validate's param is typed Any, so no
  suppression/ignore comments needed. This is the standard approach going
  forward for all *Resp schema construction from ORM query results.
- GET /books: implemented, using the model_validate pattern above.

SEED.PY - DECIDED
- Seeds User(id=1, sub="test", ...) and one Book with 10 Word rows.
  Run manually with app_context via importing app from api.py. Practice
  rows intentionally NOT seeded.

SCHEMAS.PY - DECIDED
- BookSchema needs model_config = ConfigDict(from_attributes=True) (and
  same for WordSchema, PracticeSchema) to build from SQLAlchemy objects.
- Single-book design (not list) for SyncBookReq/SyncBookResp -- corrected
  from an earlier draft that mistakenly used list[BookSchema].

STILL OPEN / NEXT STEPS
- GET /books/{id}: whole-book payload (words + practice-state, both
  directions) -- not yet implemented.
- /sync endpoint (POST): request/response shape not yet formalized.
- Apply the model_validate pattern to GET /books/{id} and /sync once
  built.
- JWT + Google OAuth (Authlib + flask-jwt-extended chosen,

WORKING STYLE (important)
Socratic, one problem at a time, concise replies. Kyosuke writes all code
himself -- do not provide code unless explicitly requested. Don't pile on
multiple follow-up questions or extra info beyond what was asked.
#+end_src

#+begin_src 
uv run python -m be.api
uv run python -m be.seed
flask --app be.api:app run --port 8000 --debug
#+end_src

*** Frontend
- Vue scaffolding
#+begin_src bash
pnpm create vue@latest fe
# include TypeScript, Router, Pinia, Linter and Prettier
cd fe
pnpm install
pnpm add -D daisyui@latest
pnpm add -D @tailwindcss/vite tailwindcss
# pnpm add -g @vue/language-server  # for Emacs
# pnpm install -g typescript-language-server typescript  # for Emacs
pnpm add -D eslint eslint-plugin-vue  # Emacs
pnpm add -D openapi-typescript
pnpm add openapi-fetch
pnpm add -D @vue/typescript-plugin # Emacs
pnpm add -D vitest
#+end_src
- add to vite.config.js
#+begin_src js
import tailwindcss from '@tailwindcss/vite'  // <---

export default defineConfig({
  plugins: [
      vue(),
      tailwindcss(),  // <---
      vueDevTools(),
  ],
#+end_src
- create fe/src/style.css
#+begin_src css
@import "tailwindcss";
@plugin "daisyui" {
    themes: emerald, light, dark;
}
#+end_src

#+begin_src 
pnpm exec openapi-typescript http://localhost:8000/openapi/openapi.json -o src/types/api.ts
pnpm exec vue-tsc --noEmit
#+end_src

- add to pyproject.toml to suppress warnings
#+begin_src 
[tool.pyright]
reportUntypedFunctionDecorator = false
reportUnknownMemberType = false
#+end_src

- context 7/22
#+begin_src 
VB2 SESSION CONTEXT (continuation — frontend)

BACKEND STATUS (done)
- Flask + flask-openapi3 (migrated from flask_pydantic; @validate() removed)
- OpenAPI spec served at /openapi/openapi.json (not /openapi.json)
- Path params: separate Pydantic model (e.g. BookPath with `id: int`),
  function param named `path: BookPath` — NOT plain `id: int` in signature
- SQLAlchemy: custom Base(MappedAsDataclass, DeclarativeBase); db =
  SQLAlchemy(model_class=Base, session_options={"expire_on_commit": False})
- Practice model: last_edited required (no default, client supplies via
  sync); last_practiced/status/due_dates/due_counter all optional with
  defaults (None/NEW/None/None) — None is valid pre-graduation state, not
  "unset"
- Endpoints implemented: GET /books, POST /books/{id} (sync_book) —
  single endpoint handles both "fetch only" (empty practices) and
  "sync local changes" (populated practices) cases
- SyncBookReq: {practices: list[PracticeSchema]}
- SyncBookResp: {book: BookSchema, words: list[WordSchema],
  practices: list[PracticeSchema]}
- sync_book reconciliation: last-write-wins per practice via last_edited
  timestamp comparison; new practices (no id) get inserted; existing
  practices only updated if cp.last_edited > sp.last_edited; book's
  wd_last_practiced/dw_last_practiced updated from max(cp.last_practiced)
  per direction

FRONTEND STATUS (starting now)
- Vue 3 + TypeScript + Pinia + DaisyUI, scaffolded, pnpm
- Tailwind v4 + @tailwindcss/vite installed
- src/types/api.ts generated via:
  pnpm exec openapi-typescript http://localhost:8000/openapi/openapi.json -o src/types/api.ts
- Vitest/E2E testing deliberately deferred until main flow is working
  end-to-end

STILL OPEN
- All frontend work: API client setup, stores, views, sync-call
  integration, offline local-changes collection
- JWT + Google OAuth (Authlib + flask-jwt-extended chosen, not implemented)
- Auth currently hardcoded user_id=1 server-side

WORKING STYLE (important)
Socratic, one problem at a time, concise replies. Kyosuke writes all code
himself — do not provide code unless explicitly requested.
#+end_src

- src/vite-env.d.ts
#+begin_src js
/// <reference types="vite/client" />

interface ImportMetaEnv {

interface ImportMeta {
  readonly env: ImportMetaEnv
}
#+end_src

*** Pinia structure (rev0)
- booksStore
  - books: BookSchema[]
  - activeBook: BookSchema
  - fetchBooks() => BookSchema[]
  - deleteBook() => bool
  - editBook() => BookSchema
  - creaetBook() => BookSchema  # do I need this?
- wordsStore
  - words: WordSchema[]
  - fetchWords() => WordSchema[]
  - editWord() => WordSchema
  - createWord() => WordSchema # do I need this?
- pracsStore
  - pracs: PracticeSchame[]
  - syncBook() => activeBook, words, pracs are updated

*** Wiring up
- fe/index.html
#+begin_src html
<html lang="en" data-theme="bumblebee">
#+end_src

- App.vue
#+begin_src vue
<template>
  <RouterView />
</template>
#+end_src

- main.ts
#+begin_src js
import './style.css'
#+end_src

- add icons
#+begin_src 
pnpm add lucide-vue-next
#+end_src


*** bug
- bug: waiting items will be learning when moved from ww to lw
- it should be:
  - only Okay button can set it as "waiting"
  - when moves an item from ww to lw:
    - "waiting" -> should stay "waiting"
    - others -> changed to "learning"
  - when 'new' items go into ww -> should stay 'new'


- context 7/27
#+begin_src 
VB2 SESSION CONTEXT HANDOFF — frontend, PracView.vue debugging

BACKEND (stable)
- Flask + flask-openapi3, flask-cors added (CORS(app, origins=["http://localhost:5173"]))
- POST /sync/{id} (renamed from /books/{id}) — full book+words+practices sync
- GET /books/{id}/words — lighter endpoint (book+words only)
- create/edit/delete book & word endpoints: NOT built yet

FRONTEND ARCHITECTURE
- Vue 3 + TS + Pinia (single store: src/stores/books.ts) + DaisyUI + Tailwind v4
- Store state: books, words, practices (flat, separate top-level refs, NOT nested), activeBookId, pracDir ('wd'|'dw')
- PracView.vue: local refs — lw/ww (number[] of practice indices), wordsNoPrac (number[] of word indices),
  lastSyncTime (Date|null), pracIdx (number|null, currently-practicing index)

QUEUE CONVENTION (finalized, was previously inconsistent — now fixed everywhere):
  push = add to TAIL, shift = remove from HEAD (standard FIFO)

CORE LOGIC (all implemented, most bugs fixed):
- id2ixWord(wid)/id2ixBook(bid): id→array-index lookup, uses .entries() correctly now, returns null if not found — ALWAYS check `=== null`, not truthiness (index 0 is falsy!)
- isNextDayOrLater(referenceDate, now): has null-guard now (returns false if referenceDate == null) — fixed bug where null lastSyncTime caused sync to fire on EVERY click, not just first
- syncServer(): calls booksStore.syncBook(), rebuilds wordsNoPrac, decrements due_counters (only if isNextDayOrLater true), sets lastSyncTime.value = new Date()
- moveToWins(pracs): shuffles + fills lw (to LWSIZE=5) then ww (to WWSIZE=5) from a candidate list; status overwrite guards now correctly preserve 'waiting'/'review' (only sets 'learning' if neither)
- statusMove(status): pulls all pracs matching a status into moveToWins
- fillWins(): single-pass (not looping) fallback chain: due reviews (due_counter==0) → shift ww-to-lw → statusMove('learning') → statusMove('waiting') → statusMove('new') → create practices from wordsNoPrac. NO fallback to non-due reviews (deliberate design choice — see below). Falls through to "quit" state if lw ends up empty/short.
- doPrac(): syncs if needed, fillWins(), sets pracIdx = lw.value[0] (head) or null if lw empty — must reset pracIdx to null explicitly when lw is empty (recently added)
- onceMore(): lw.value.push(lw.value.shift()) — head to tail, no status change
- okay(): review→push to ww tail; waiting→review (due_dates=due_counter=1), shift from lw; new/learning→waiting, push to ww tail
- memorized(): review→double due_dates, due_counter=due_dates; else→review, due_dates=due_counter=1; shift from lw

DESIGN DECISION (just made): do NOT allow practicing non-due review items to pad lw/ww (avoids
premature due_dates inflation exploit). If nothing left to fill windows, show "No due items to
learn today. Well done!" empty state instead. lw.length==0 → empty state.

TEMPLATE STATE
- Card flip (isFlipped), Transition-wrapped front/back content
- Buttons (Once More / Okay / Memorized) disabled via `pracIdx === null` (NOT truthy check — 0 is valid index)
- Empty state `v-if="lw.length > 0"` .. `v-else` "No due items to learn today. Well done!"
- TTS buttons (Word/Definition/Sample) — click handlers not yet wired, just disabled-state logic done
- Known non-bug: Emacs eglot shows stale TS2307 false-positive errors on some files (ignore)

WORKING STYLE
Socratic, one problem at a time, concise. Kyosuke writes all code himself — don't provide code
unless explicitly requested. Answer immediate question only, no unsolicited follow-ups.

CURRENT STATUS: Core practice loop logic + empty-state handling verified working end-to-end via
manual testing (console.log + Flask debug console + Pinia devtools). Moving to continued UI
debugging in a fresh chat — TTS button wiring is likely next real feature to build.
#+end_src

#+begin_src 
        BookSchema: {
            dw_last_practiced: string | null;
            last_edited: string;
            wd_last_practiced: string | null;

        PracticeSchema: {
            last_edited: string;
            last_practiced: string | null;

        WordSchema: {
            last_edited: string;
#+end_src

- context 7/28 am
#+begin_src 
VB2 SESSION CONTEXT HANDOFF — auth (Google OAuth + JWT), new chat

STATUS: Frontend PracView.vue core practice loop is stable (dedup bugs, queue
rotation, tz-aware datetime sync bugs all fixed this session). Sync verified
working end-to-end with cleared DB. Moving to auth implementation next.

BACKEND (stable, pre-auth)
- Flask + flask-openapi3, flask-cors (CORS origins=["http://localhost:5173"])
- POST /sync/{id} — full book+words+practices sync, last-write-wins per field
  via last_edited timestamp comparison
- GET /books/{id}/words — lighter endpoint (book+words only)
- create/edit/delete book & word endpoints: NOT built yet
- DB: SQLite via SQLAlchemy, MappedAsDataclass + DeclarativeBase custom Base
- All datetime columns are DateTime(timezone=True), but SQLite doesn't
  actually persist tzinfo — convention is aware-UTC in Python (.astimezone(UTC),
  no tzinfo stripping), stored naive-but-UTC-valued in SQLite

AUTH PLAN (decided, not yet implemented)
- JWT via flask-jwt-extended
- Google OAuth via authlib
- Deferred until now — no auth currently on any endpoint
- user_id already exists as a field on Word/Practice (currently presumably
  hardcoded or unenforced client-side)

FRONTEND ARCHITECTURE
- Vue 3 + TS + Pinia (single store: src/stores/books.ts) + DaisyUI + Tailwind v4
- openapi-typescript generated client for API calls (see syncBook() pattern)
- Store state: books, words, practices (flat, separate top-level refs) + activeBookId, pracDir

WORKING STYLE
Socratic, one problem at a time, concise. Kyosuke writes all code himself —
don't provide code unless explicitly requested. Answer immediate question only.
#+end_src

- context 7/29 am
#+begin_src 
VB2 SESSION CONTEXT HANDOFF — auth (Google OAuth + JWT) COMPLETE, new chat
STATUS: Full Google OAuth + JWT flow verified end-to-end (login → token
issued → protected /books fetch → real data rendered). Ready to move to
create/edit/delete endpoints next.

BACKEND (auth complete)
- Flask + flask-openapi3, flask-jwt-extended, google-auth (id_token.verify_oauth2_token)
- python-dotenv loaded in app.py; .env holds JWT_SECRET_KEY, GGL_CLIENT_ID (gitignored)
- app.config["JWT_SECRET_KEY"], JWTManager(app), JWT_ACCESS_TOKEN_EXPIRES=timedelta(days=14)
- User model exists (id, sub, email, name, books/practices relationships)
- POST /auth/ggl — verifies Google ID token via id_token.verify_oauth2_token(
  body.token, requests.Request(), os.getenv("GGL_CLIENT_ID")), 401 on ValueError,
  looks up/creates User by sub, create_access_token(identity=str(user.id))
  [KEY GOTCHA: identity must be str, not int — "Subject must be a string" error otherwise]
- GglAuthReq{token: str} / GglAuthResp{user_id, access_token, email, name}
- @jwt_required() + get_jwt_identity() (cast to int) added to /books, 
  /books/<int:id>/words, /sync/<int:id>
- Ownership checks: /books/<id>/words and /sync/<id> verify Book.user_id ==
  current user via single query: select(Book).where(Book.user_id==uid, Book.id==path.id),
  return 404 if not found (not 401, avoids leaking existence)
- create/edit/delete book & word endpoints: still NOT built

FRONTEND (auth complete)
- vite.config.ts: server.proxy forwards /auth, /books, /sync → http://localhost:8000
  [Flask runs on 8000, Vite dev server on 5173 — needed or all backend calls 404]
- VITE_GOOGLE_CLIENT_ID in .env (Vite requires VITE_ prefix to expose to client code)
- LoginView.vue: loads GIS script dynamically in onMounted, renderButton, 
  handleCredentialResponse posts idToken to /auth/ggl, on success calls
  userStore.setUser(data) + router.push('/books') (setUser only called inside
  the res.ok branch)
- stores/user.ts (useUserStore, Pinia setup store): state = user_id, email,
  name, access_token; setUser(user: GglAuthResp) writes state + localStorage
  key 'user'; loadUser() reads it back — side-effect persistence pattern
  (store doesn't wrap localStorage internally, just touched at login/boot)
- main.ts: useUserStore() + loadUser() called AFTER app.use(pinia)/app.use(router)
  but BEFORE app.mount('#app') — avoids flash of logged-out state
- stores/books.ts: fetchBooks() action sends Bearer ${userStore.access_token}
  header via generated openapi-typescript client, assigns to store's own books ref
- Router: /login, /books (new), /words, /prac all registered
- Logout: clears Pinia state + localStorage, redirects to LoginView (no Google
  disableAutoSelect call) — NOT YET IMPLEMENTED as an action, was only discussed

DECIDED, NOT YET DONE
- No refresh token (single 14-day access token, accepted tradeoff)
- localStorage over httpOnly cookie (accepted XSS tradeoff, personal project)
- logout() action not yet written

WORKING STYLE
Socratic, one problem at a time, concise. Kyosuke writes all code himself —
don't provide code unless explicitly requested. Answer immediate question only.
#+end_src


*** Import problem
**** Problem
- main.ts
  - import router from './router' runs router/index.ts
    - import PracView runs it
      - imports booksStore
        - imports client.ts
          - using useUserStore() here is problematic (pinia is not ready)
  - app.use(createPinia())

**** Fix
- don't call useUserStore() at module scope -> should be in onResponse
#+begin_src js
const authMiddleware: Middleware = {
  async onResponse({ response }) {
    if (response.status === 401) {
      const userStore = useUserStore()
      userStore.unsetUser()
      router.push({ path: '/login', query: { redirect: router.currentRoute.value.fullPath } })
    }
    return response
  }
}
#+end_src

*** Adding modal
- define modal ref
#+begin_src js
const modalRef = ref(null)
#+end_src
- define open&close modal functions
#+begin_src js
const openModal = () => {
  modalRef.value?.showModal()
}

const closeModal = () => {
  modalRef.calue?.close()
}
#+end_src
- add button, etc. to html
#+begin_src html
<button @click="openModal" class="btn btn-secondary btn-outline btn-sm rounded-3xl ml-4">Add something</button>
#+end_src
- add dialog(modal) in html
#+begin_src html
<dialog ref='modalRef' class='modal modal-bottom sm:modal-middle backdrop:backdrop-blur-sm transition-all duration-300'>
  <div class='modal-box p-6 max-w-lg rounded-2xl border border-base-200/60 shadow-xl bg-base-100/95 backdrop-blur-md'>
    <!-- Contents here -->
  </div>
  <form method='dialog' class='modal-backdrop'>
    <button @click='closeModal'>close</button>
  </form>
</dialog>
#+end_src

- context 8/12
#+begin_src 
VB2 SESSION HANDOFF

DONE THIS SESSION
- goHome sync-before-navigate bug fixed (activeBookId was nulled before syncServer ran)
- syncServer/createWordsNoPrac/wordsNoPrac/lastSyncTime moved from PracView.vue into booksStore
  (avoids <script setup> export error, shared between Navbar.vue and PracView)
- fixed deleteWord bug: was pracs.value.splice(wix,...) instead of words.value.splice(wix,...)
- logout implemented: userStore.unsetUser() (clears refs + localStorage.removeItem('user')),
  then router.push('/login')
- JWT 401 handling: client.ts authMiddleware (client.use) checks response.status===401,
  calls useUserStore() INSIDE onResponse (not module scope - Pinia not installed yet at
  client.ts's import time, since router/index.ts static-imports views -> stores -> client.ts,
  all before app.use(createPinia()) in main.ts), then redirects to /login with
  ?redirect=<current path>
- post-login redirect: LoginView reads route.query.redirect, pushes there instead of
  hardcoded '/books'
- book CRUD UI done (add/edit/delete), backend + frontend, using {status, message} return
  pattern from store actions (e.g. deleteBook) so views can alert specific error text
- backend: added MessageResp schema ({message: str}) declared in responses={} dict for
  edit_book/delete_book 400/404 so error.message types correctly on frontend (was falling
  back to auto-generated 422 ValidationErrorModel[] type, which has no .message field)
- pattern for endpoints with declared message-based errors: error is a union including the
  422 shape; narrow with 'message' in error before accessing error.message (422 not handled,
  no need to special-case beyond the narrowing)

KNOWN NOISE (not fixed, not blocking)
- @jwt_required() triggers reportAny in Emacs/pyright on every protected route (untyped
  decorator in flask-jwt-extended stubs) - pre-existing, not worth chasing right now

WORKING STYLE
Socratic, one problem at a time, concise. Kyosuke writes all code himself - don't provide
code unless explicitly requested. Answer immediate question only, no unsolicited follow-ups.
#+end_src

*** To Dos
**** backlog
- <done>add alerts \to toast
- <done>add/edit/delete word
- <done>logout
- <done>JWT expiration
  - Should call userStore.unsetUser() and redirect to /login page
  - Before calling /login, need to capture where you are
- <done> modernize login view
- <done> add/edit/delete book
- <done> Show username in navbar
- <done> export/import all (books, words and pracs)
- <done> Converter of vb export file to vb2
- Middleware injects authorization header

**** bug
- <fixed>words of learning are reset to new after quit/restart practice
- <fixed>can't login

*** Data structure and logic
**** data structure (will use index, not ID)
- const lw = ref<number>[]([]) // learning window
- const ww = ref<number>[]([]) // waiting window
- wordsNoPrac: int[]  // words without practice (for the direction)
- lastSyncTime: DateTime

**** status
- new - just created.  not in LW???
- learning - in LW.  If OK -> waiting
  - OK -> waiting
  - Once more -> learning
  - Memorized -> review
- waiting - in LW or WW
  - OK, Memorized -> review (due_dates = 1)
  - Once more -> learning
- review
  - OK -> put to tail of WW
  - Memorized -> due_dates *2
  - Once more -> learning

**** overall flow
- sync with server
- decrement due counters (0 or -1)
- create wordsNoPrac
- fill lw and ww
- (practice)
- (if +1 day, sync with server)
- on leaving practice, sync with server

**** Filling lw and ww (for the direction)
- if lw.length > 5 -> move to ww
- if ww.length > 5 -> remove from ww
- for steps below, if lw.length == ww.length == 5 -> quit
- search prac with status 'review' and due_cnt == 0
- put to lw -> ww
- if lw.length < 5, move from ww until (lw.length == 5 or ww.length == 0)
- search prac with status 'learning'
- put to lw -> ww
- search prac with status 'waiting'
- put to lw -> ww
- search prac with status 'new'
- put to lw -> ww
- from wordsNoPrac, create practice with 'new'
- put to lw -> ww

**** Practice
- check if lastSyncTime > 1 day then sync with server
- fill lw and ww
- remove one at head of lw
  - once more -> put at tail of lw
    - status * -> learning
  - ok
    - status == new | learning -> status = waiting, put at tail of ww
    - status == waiting -> status = review, init due_dates/counter
    - status == review -> put at tail of ww
  - memorized
    - status == review -> due_dates x 2, due_cnt = due_dates
    - status others -> status = review, due_dates = dut_cnt = 1
- loop from top

**** Bugs
- <done> Why are new items at head of LW?
- <done> Spec bug - review items are just put to tail of WW when Okayed
- <done> Remaining decreased when once more was clicked?? +1
  - (Before that,) No due decreased when okay was clicked
- <done> I see non-zero due_counter (& review) at head of lw
- <done> When I quit prac session and restart, due went 30 to 0
  - session start -> quit -> restart: due is 30 (correct)
  - session Okay for 1 prac -> quit -> restart: due is 30 -> 0 (wrong!)
    - After Okay and quit, the Okay-ed prac is correct due_cnt = due_dates = 2 in DB
    - When resumed a new session, the Okay-ed prac is correct due_cnt = due_dates = 2 in DB
    - I see prac id=41 was reset from due_cnt=0 to 1.  The DB should have been updated to due_cnt=0 on quit session.
    - I see its last_edited = last_practiced in DB.  last_edited should have been updated.
    - I see the same on Pinia.
  - -> root cause: I didn't update last_edited when decrementing due_cnt

#+begin_src 
Can you help me fix a frontend bug in my flashcard web app?  It's written in TS and Vue.  I have books, words and pracs (practices) in Pinia.  A word corresponds to two pracs (word -> definition & def -> word).  I have LW (Learning Window) and WW (Waiting Window) both of which have index number of pracs.  
LW has 5 pracs that user is actively memorizing.
WW has 5 pracs that will be moved to LW.
pracs have status, 'learning', 'waiting', 'review' and 'new'.
'learning' means the user is actively memorizing it
'waiting' means that if user clicks 'Okay' again, it will be 'review'
'review' means it can reappear after due_dates dates.  due_counter is used to track how many days remains until the prac reappear.
'new' means the prac is created and new.  when it moves to LW, the status becomes 'learning'.
All pracs in LW/WW with status='review' should have due_counter=0 which means it's due for review.
The bug is that sometimes I see a prac (or two) with status=review and due_cunter!=0 in LW/WW.  It looks that such a prac suddenly appear in LW or WW during practice lesson.  I display due_counters of pracs in LW and WW.  Usually, they are:
0 0 0 0 0 <- LW
0 0 0 0 0 <- WW
But during a practice session, I suddenly see like:
0 0 2 0 0
0 0 0 0 0
This is still part of context.  I will paste 2 files.  Hold until I paste them.
#+end_src

- 8/21/2026
#+begin_src 
I think I've fixed outstanding bugs.  Let's resume in a new chat.  Give me a short context in a code snippet.
Do you have a recommendation on what I should work on now?
From backlogs:
- DB migration
- Add testing
- Hosting at pythonanywhre
#+end_src

- 8/27
#+begin_src 
pnpm test
#+end_src

- context (8/28/2026)
#+begin_src 
VB2 SESSION HANDOFF — moving to backend tests

CONTEXT
Frontend Vitest suite for PracEngine (pracengine.ts / pracengine.spec.ts)
is done — full branch coverage on okay()/memorized()/onceMore()/fillWins(),
including the FIFO ww-promotion edge cases. Kyosuke is satisfied with FE
coverage for now.

NEXT
Shift to backend test coverage.

BACKEND STACK
- Flask / flask-openapi3
- SQLAlchemy / SQLite
- flask-jwt-extended
- Google OAuth (frontend-token-handoff: id_token.verify_oauth2_token)

KNOWN PATTERNS TO KEEP IN MIND WHEN WRITING TESTS
- Ownership checks embedded in queries return 404, not 401
- model_validate(..., from_attributes=True) for ORM -> Pydantic
- Separate CreateXReq / PatchXReq / XSchema models
- Datetime normalization: .astimezone(UTC).replace(tzinfo=None) for
  SQLite-safe comparisons
- field_serializer on schemas attaches UTC markers on outbound responses
- Export/import: ExportResp shared schema, .model_dump(mode="json")
  needed to run serializers before Flask touches the data
- Auth middleware (client.ts, frontend side) calls useUserStore() inside
  onResponse, not module scope — not backend, but relevant context if
  auth flows come up

STATUS
Full CRUD surface for books/words is complete on both backend and
frontend. No backend test scope decided yet — first task next session
is to pick where to start (e.g. auth/ownership checks, datetime
normalization, export/import round-trip).

WORKING STYLE
Socratic, one problem at a time, concise. Kyosuke writes all code
himself — don't provide code unless explicitly requested. Answer
immediate question only, no unsolicited follow-ups.
#+end_src

#+begin_src 
uv add --dev pytest
#+end_src

**** bug
- If I click once more for the only word prac which is 'waiting', it doesn't flip back

** Deploying to PythonAnywhere
*** Tear down vbs
- Disable webapp from Web tab
- Open bash console on PA
#+begin_src 
pwd
# /home/achiwa912
ls -la vbs
rm -rf vbs
#+end_src

*** Build FE locally
#+begin_src 
pnpm install  # already did
pnpm build
#+end_src
- add fe/dist to git

*** Change required python version
- Change required python version (was >=3.14) in pyproject.toml
#+begin_src 
requires-python = ">=3.13"
#+end_src

#+begin_src 
uv lock
git add pyproject.toml uv.lock  # I use magit for these
git commit -m "Allow Python 3.13"
git push
#+end_src*


*** Update PW system image
- https://help.pythonanywhere.com/pages/ChangingSystemImage
- Go to Account > System Image
  - I'm using =haggis= which supports up to Python 3.10
  - I need to change it to =innit= which supports 3.13
  - https://help.pythonanywhere.com/pages/PythonVersions
- Change current system image to innit
- Change all default python version to 3.13
- vim vb2/.python-version
#+begin_src 
3.13 # from 3.14
#+end_src
- fresh start
#+begin_src 
rm -rf ~/*
rm -rf ~/.*
rm -rf /tmp/*
#+end_src
- start a new bash console
#+begin_src 
cd ~/
mkvirtualenv vb2-venv
git clone https://github.com/achiwa912/vb2.git
# pip install uv  # already there
cd vb2
rm README.org # to avoid uv error (.org is not supported)
UV_PROJECT_ENVIRONMENT=/home/achiwa912/.virtualenvs/vb2-venv uv sync --no-install-project --python 3.13
#+end_src

*** create a new webapp
- delete achiwa912.pythonanywhere.com
- add a new web app > Manual configuration > Python 3.13
- Source code: /home/achiwa912/vb2
- Working directory: /home/achiwa912/vb2
- Virtualenv: =/home/achiwa912/.virtualenvs/vb2-venv=
- vim /var/www/achiwa912_pythonanywhere_com_wsgi.py
#+begin_src python
import sys
import os
from dotenv import load_dotenv

project_folder = os.path.expanduser('~/vb2')
load_dotenv(os.path.join(project_folder, '.env'))

path = '/home/achiwa912/vb2'
if path not in sys.path:
    sys.path.insert(0, path)

os.chdir(path)

from be.api import app as application
#+end_src
- vim .env on PA
#+begin_src bash
export SERVE_FRONTEND=1
export CORS_ORIGINS=http://localhost:5173,https://achiwa912.pythonanywhere.com
export JWT_SECRET_KEY=<snip>
export GGL_CLIENT_ID=<snip>
#+end_src
- 
#+begin_src bash
# uv pip install python-dotenv  # already installed
echo 'set -a; source ~/vb2/.env; set +a' >> ~/.virtualenvs/vb2-venv/bin/postactivate
#+end_src

*** Google Cloud Console
- Login to GCC > APIs & Services > Credentials > vocabull > add https://achiwa912.pythonanywhere.com


** Make cards swipable (on mobile)
- use Vue transition
- Context (9/14)
#+begin_src 
CONTEXT FOR CONTINUATION \u2014 vb2 Flashcard App Mobile UI

## Project
- Repo: https://github.com/achiwa912/vb2 (frontend in `fe/`)
- Stack: Vue 3 + Vite + TypeScript + Tailwind CSS v4 + DaisyUI 5 (theme: bumblebee)
- Deployed on PythonAnywhere at https://achiwa912.pythonanywhere.com
- Backend serves SPA from `~/vb2/fe/dist`; static mapping:
    /assets/  ->  /home/achiwa912/vb2/fe/dist/assets
    /         ->  /home/achiwa912/vb2/fe/dist
- `fe/src/style.css` contains only:
    @import "tailwindcss";
    @plugin "daisyui" { themes: bumblebee, light, dark; }
- No tailwind.config.js (v4 doesn't need one).
- `App.vue` is just `<RouterView />` \u2014 no shared layout shell.

## Goal
Make the app usable on mobile (currently designed desktop-first, looks bad
on small screens).

## Already fixed
1. Navbar.vue \u2014 "vocaBull" text hidden on mobile (`hidden sm:inline`),
   logo (vB) still shown.
2. PracView.vue \u2014 stat card made responsive: `mx-3 sm:mx-8 md:mx-16`,
   `grid-cols-2 sm:grid-cols-4`, label/value split into stacked spans on
   mobile, `text-sm sm:text-base`.
3. PracView.vue \u2014 practice card wrapper: `mx-16` -> `mx-3 sm:mx-8 md:mx-16`,
   `h-[300px]` -> `min-h-[280px] sm:min-h-[300px]`.
4. PracView.vue \u2014 action buttons: container changed from
   `flex justify-center gap-2` to `grid grid-cols-3 gap-2 w-full`;
   buttons `btn-lg` -> `btn-sm sm:btn-lg`; `ml-8` on Memorized ->
   `sm:ml-8`.
5. Resolved a stale-build blank-page issue (index.html referenced old
   asset hashes; rebuilt and re-deployed).

## Currently in progress
Mobile UI polish, working through PracView.vue issues one at a time.

### Just completed
- "Once More" button was wrapping to 2 lines on mobile. Decided the fix
  but NOT yet applied: use `<span class="sm:hidden">More</span>` +
  `<span class="hidden sm:inline">Once More</span>` inside the button
  (same pattern for Okay -> "OK", Memorized -> "Done" if needed).
  PENDING: user needs to confirm/apply this change.

### Next major feature: Level 1 card stack
User wants a Tinder-style card stack on PracView:
- Active card on top; 1 blank ghost card behind it (no text content in ghost)
- Vertical offset for ghost (translateY upward)
- Ghost is purely decorative \u2014 blank card shape, dimmed
- Promotion animation via Option A: rely on Vue reactivity + CSS transition
  (ghosts are stateless, always rendered from current engine state; when the
  engine index changes, Vue re-renders and CSS transitions handle the
  promotion automatically \u2014 no manual timing coordination)
- Later: swipe gestures (right = engine.okay(), left = engine.onceMore()),
  tap = flip. Pointer Events API, `touch-action: pan-y` on card.
- Swipe threshold ~100px; tap threshold ~8px; rotation = dx/20 degrees.

## Card stack plan (agreed, not yet started)
Step 1 (next): Add 2 absolutely-positioned ghost divs behind the active
  card in PracView.vue. No gestures, no animation yet. Values:
    ghost 1: translateY(-8px)  scale(0.95)  opacity 0.7  z-index 2
    ghost 2: translateY(-16px) scale(0.90)  opacity 0.4  z-index 1
    active:  normal flow, z-index 3
  Container becomes `relative`; ghosts use `absolute inset-0` +
  `pointer-events-none`; container `mt-6` instead of `mt-4`.
  Ghost bg: `bg-base-200` (dial to `bg-base-300` if not visible).
Step 2: Add `hasNext` boolean to PracEngine so ghosts render conditionally.
Step 3: Fly-off animation on swipe release past threshold (swipe.exitDir
  state, transitionend to sequence engine advance).
Step 4: Verify ghost promotion looks smooth on real phone.
Step 5: Fade out ghosts when deck is empty (empty state transition).
Step 6: Add swipe gesture handlers (pointerdown/move/up) + tap-to-flip
  with movement threshold.

## Design decisions locked in
- Ghost cards: blank (no real content) \u2014 keeps Step 1-2 simple
- Vertical offset (not horizontal)
- Option A for ghost promotion (reactive, no TransitionGroup)
- Memorized button stays tap-only (no swipe gesture)
- Keep Okay/Once More/Memorized buttons visible as accessibility fallback
- Respect prefers-reduced-motion (instant swap for those users)
- Pointer events (not separate mouse/touch listeners)

## Reference links the user has
- https://motion.dev/examples/vue-card-stack
- https://ruixen.com/components/customer-story-stack
- https://fwdtools.com/ui-snippets/swipe-cards/
- MDN: Pointer events, touch-action, transform, perspective,
  prefers-reduced-motion
- Vue docs: <Transition>, transition hooks, explicit durations

## Immediate next action
User is about to paste the CURRENT PracView.vue practice-card block (from
`<!-- practice card -->` through the empty-state closing div). Next
assistant should provide the Step 1 replacement code (ghost cards, no
gestures/animation yet) that drops in cleanly against the pasted code.
#+end_src

*** show ghost cards
- add relative and z-<number> to active card
- ghost cards will have the same shape as outer div, smaller z-<num>
#+begin_src html
    <!-- practice card -->
    <div class="relative flex items-center justify-center mt-6">

      <!-- ghost cards -->
      <div
	v-if="engine.lw.length > 2"
	      class="absolute inset-y-0 left-3 right-3 sm:left-8 sm:right-8 md:left-16 md:right-16 rounded-3xl border border-base-300 bg-base-200 pointer-events-none z-10"
	      style="transform-origin: top center; transform: translateY(-16px) scale(0.98); opacity: 0.4;"
      ></div>
      <div
	v-if="engine.lw.length > 1"
	class="absolute inset-y-0 left-3 right-3 sm:left-8 sm:right-8 md:left-16 md:right-16 rounded-3xl border border-base-300 bg-base-200 pointer-events-none z-20"
	style="transform-origin: top center; transform: translateY(-8px) scale(0.99); opacity: 0.7;"
      ></div>

      <div v-if="engine.lw.length > 0" class="card card-lg bg-base-100 w-full min-h-[280px] sm:min-h-[300px] border border-base-300 rounded-3xl shadow-sm mx-3 sm:mx-8 md:mx-16 select-none flex flex-col justify-between hover:border-base-content/24 hover:shadow-xl transition-all duration-200 relative z-30">
#+end_src
- don't need hasNext it's just lw.length =0, >0 (active only), >1 (one ghost), >2 (2 ghost)

*** fly-off animation w/ existing buttons
- define exitStyle (animation none or flying off right/left) and set to active card
- define action handler (onAction) and assign to Once More and Okay buttons

*** swipe
- changed exitStyle to cardStyle


** backlog
*** <DONE> redesign card layout
- voice play buttons
  - now: 3 - word, def, sample -> to: 2
    - 'front' - what is on front (eg, word)
    - 'answer' - the rest (eg, def & sample; disabled if front is shown)
  - move voice play buttons on top of cards

*** <DONE> autoplay
- Have a toggle switch on prac view (initially disabled, not persisted)
- Toggling it enables autoplay, and play the current card (front or back)
- front - plays what is on front
- back - plays the rest (word, definition, sample minus what is on front)
- flipping card (can be many times) or moving to the next card triggers play

*** improve stats
**** <DONE> prac
- now: (1) this session correct/not, (2) remaining today, (3) due today, (4) due within 3 days
- then: (1) v (correct) x (not) (2) <num> left (3) +<num> coming up
**** <DONE> words view
- mastery: due_dates >= 8 (&& status=='review')
- bar mastery / all
- num review, num learning+waiting, num no pracs+new for W2D and D2W
**** books view
- aggregate all books
- num books
- num review, num learning+waiting, num no pracs+new for W2D and D2W
**** practice history (books view)
- github-like heatmap
- practiced days in past 2 weeks
**** hardest word list (words view)
- need to add ok(okay&memorized) and ng(once more) counters to Practice DB table
**** db changes
- need History table
  - date # GMT; duration 24 hours
  - book_id
  - direction
  - ok_counter
  - ng_counter
- add columns to Practice table
  - ok_counter
  - ng_counter

#+begin_src 
I'd like to show a github-like heatmap which shows activities of past 2 weeks, and lists of challenging words (eg, num of total once mores) for each book.
I think I need a new table, something like Session, Sync or (daily practice) History.  Either way, I'd like to be able to aggregate how many okays and once mores I had in each day.  The problem is, though sync can send ok/once_more counters, the numbers I currently have are not reset (at frontend) until the session ends.  Also, multiple devices could sync.
#+end_src

*** <DONE> offline mode
- If you are in words view or prac view, and book.words and book.pracs are loaded, you can practice the book offline
- To do so, I need to sync book when user enters a words view.  Currently, only book.words are loaded.

*** <DONE> undo
- infoTried, infoMem, isFlipped, lw, ww, pracIdx
- prev pracs[lw[0]], new pracs[lw[lw.length-1]]
  - -> all prev pracs[lw[0]], pracs[ww[0]]
- if resetWindows in doPrac, clear snapshot array for guard

*** <DONE> add edit word on PracView

*** CSV import

*** keyboard shortcut
- h, <- - once more
- j, (lower arrow) - memorized
- k, (upper arrow), space - flip card
- l, -> - okay
- n - play front
- m - play answer
- A - toggle auto-play
- Esc - words view (Esc again \to books view)
- U - undo



*** bugs (9/21)
- <done> browser tab name is Vite App
- <done> Book name (the same one) isn't shown on the words view title
- <done> if lw.length == 1, autoplay doesn't play
- <done> when flipping left (==once more) cards, sometimes it fires moving back to another page

#+begin_src 
vb2 (vocaBull) — context for next session
==========================================

Recently fixed:
- Mobile swipe-vs-scroll conflict (PracView.vue): back-face div's
  overflow-y-auto/max-h-[220px] competed with card's touch-action:pan-y,
  causing pointercancel mid-swipe. Removed overflow/max-h — fixed.
- Stale index bug after word delete: splice(engine.pracIdx,1) on
  booksStore.pracs left engine.lw/pracIdx stale. Fixed by calling
  engine.resetWindows() right after the splice.
- Undo (okay/onceMore/memorized) implemented in PracEngine:
  - saveUndo() at top of each action, snapshots (all copied, not
    referenced): infoTried, infoMem, isFlipped, pracIdx, lw (spread),
    ww (spread), praclw0 ({...pracs[lw[0]]}), pracww0 (shallow copy or
    null if ww[0] undefined)
  - undo() pops undoArray, restores fields, writes back copies of
    praclw0/pracww0 into booksStore.pracs
  - resetWindows() clears undoArray (handles day-boundary reset case)
  - No cap on undoArray depth (sessions realistically small)
  - Deliberately NOT tracking new->learning status flips on
    newly-pulled-in pracs elsewhere in lw/ww — accepted as low-impact
- Fixed "Pinia called before app.use(pinia)" error: pracengine.ts had
  a stray module-level `const booksStore = useBooksStore()` (line 8)
  plus bare `booksStore` references inside class methods (e.g. in
  saveUndo). Fix in progress: replace bare booksStore refs with
  this.store, delete the module-level call, revert the main.ts
  pinia-export workaround.
- Fixed book title not showing for book id==1: `if (!bix) return null`
  treated index 0 (falsy) as "no book". 
- Browser tab title still needs updating in index.html (<title>vocaBull</title>) — mentioned, not yet confirmed done.
- Added `overscroll-behavior-x: none` on html/body in style.css to try
  to stop Android/Firefox edge-swipe-back misfiring when the user
  retries a missed left-swipe (once-more) with a bigger gesture.
  Just applied — untested pending real-device retry.

Backlog / open items:
1. edit word feature on PracView (requested by daughter's feedback) —
   not started; need to decide UI/flow (modal? inline?).
2. Verify overscroll-behavior-x fix actually stops back-navigation
   misfires on Pixel 7xl / Firefox.
3. Finish pracengine.ts booksStore -> this.store cleanup (confirm
   constructor assigns this.store = store).
4. Books-view aggregate stats (num books, review/learn/new totals
   across books) — deprioritized ("satisfaction" not "motivation").
   Needs server-side aggregation since frontend only loads one book's
   pracs at a time. Not started, not fully spec'd.
5. Practice-history heatmap (GitHub-style, past 2 weeks) — needs new
   sessions/history table. Parked: conflicts with current
   last-write-wins-per-prac sync model since there's no delta/increment
   tracking yet (two-device offline sync would clobber counts).
6. Hardest-word list (by once-more count) — needs num_once_more/
   num_okay counters on practices table. Same parking reason as #5.
7. Migration strategy for sessions table + new counters on a live
   PythonAnywhere deployment — Alembic vs manual ALTER TABLE, no
   decision made, user has never used Alembic.

Words-view stats (mastery = status=='review' && due_dates>=10, per
direction wd/dw, plus review/learn/new counts) — DONE, per-book scoped
via bix.

Offline practice support — DONE: loading a book/starting practice
loads all words+progress to device; must sync before switching devices
or unsynced progress is at risk.

Sync model: sends all pracs, receives all words+pracs, last-write-wins
per prac (no increments currently).
#+end_src

*** CSV import
- new endpoint ~/importcsv~
  - for each line
    - try to parse as csv '\t,;'
    - skip if error
    - skip if existing; case insensitive
    - add a word if successful 
    - return num imported, skipped, failed, failed list[line_num, word]

#+begin_src 
pnpm exec openapi-typescript http://localhost:8000/openapi/openapi.json -o src/types/api.ts
#+end_src

*** Keyboard shortcuts
- 9/22
#+begin_src 
vb2 (vocaBull) — context for next session
==========================================

CSV bulk import: DONE (backend /importcsv/{id} endpoint + frontend
file picker + FormData upload + result modal). Notable gotchas hit
along the way:
- flask_openapi3 needs its own FileStorage (from
  flask_openapi3.models.file), not bytes or raw werkzeug FileStorage
- openapi-typescript types `format: binary` fields as `string` and
  widens tuple types (e.g. errors: [number, string][] -> 
  (string|number)[][]) — worked around with `as unknown as <Type>`
  casts at the call sites rather than fixing codegen config
- auth middleware's onResponse treats both 401 and 422 as auth
  failure and force-logs-out; this masked an unrelated 422
  validation error while debugging — worth narrowing to 401 only
  at some point

Next up: keyboard shortcuts — not started, no design decisions made
yet (which actions, which keys, scope/view-specific vs global).
#+end_src

#+begin_src 
I'd like to add keyboard shortcuts to my flashcard web app: vb2 which is written in TS+Vue3.
Here are candidates: 
- h, <- - once more
- j, (lower arrow) - memorized
- k, (upper arrow), space - flip card
- l, -> - okay
- n - play front
- m - play rest
- A - toggle auto-play
- Esc - words view (Esc again \to books view)
- U - undo

Don't go so fast.  Start with the basics that I need to understand first.
#+end_src

#+begin_src 
I want to show keyboard shortcut help when '?' is typed, then hide it once other key is typed, mouse/track pad is clicked or 10 seconds are passed.  Help doesn't swallow other keys while it's shown.

help modal(?) can hide the card, possibly okay button or the like.  user operates on keyboard when '?' is typed, and typing any key will dismiss the modal.
#+end_src


*** Preparing for users (other than me)
**** Scale
- <done> DB -> WAL, timeout=5000ms
- <done> max books=20/user
- <done> max words=1000/book

**** Security
- <done> ownership check for APIs (Book.user_id == user_id) ... looks fine
- <done> CSV import size & XSS possibilities in defs, sample sentences
- JWT storage - localStorage or httpOnly cookie? -> localStorage
  - backlog: move to httpOnly cookie
- Rate limiting for auth-endpoints - return 429 Too Many Requests
  - backlog
- <done> CORS not *
#+begin_src 
(vb2-venv) 00:40 ~/vb2 (main)$ echo $CORS_ORIGINS
http://localhost:5173,https://achiwa912.pythonanywhere.com
(vb2-venv) 00:40 ~/vb2 (main)$
#+end_src
